import fsNode = require('fs');
import { Minimatch, type MinimatchOptions } from 'minimatch';
declare function walkSync(baseDir: string, inputOptions?: walkSync.Options | (string | Minimatch)[]): string[];
export = walkSync;
declare namespace walkSync {
    function entries(baseDir: string, inputOptions?: Options | (string | Minimatch)[]): Entry[];
    interface Options {
        includeBasePath?: boolean;
        globs?: (string | Minimatch)[];
        ignore?: (string | Minimatch)[];
        directories?: boolean;
        fs?: typeof fsNode;
        globOptions?: MinimatchOptions;
    }
    class Entry {
        relativePath: string;
        basePath: string;
        mode: number;
        size: number;
        mtime: number;
        constructor(relativePath: string, basePath: string, mode: number, size: number, mtime: number);
        get fullPath(): string;
        isDirectory(): boolean;
    }
}
//# sourceMappingURL=index.d.ts.map