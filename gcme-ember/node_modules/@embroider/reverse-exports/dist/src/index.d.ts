type PkgJSON = {
    name: string;
    version: string;
    exports?: Exports;
};
type Exports = string | string[] | {
    [key: string]: Exports;
};
/**
 * An util to find a string value in a nested JSON-like structure.
 *
 * Receives an object (a netsted JSON-like structure) and a matcher callback
 * that is tested against each string value.
 *
 * When a value is found, returns an object containing a `value` and a `key`.
 * The key is one of the parent keys of the found value — the one that starts
 * with `.`.
 *
 * When a value is not found, returns `undefined`.
 */
export declare function _findPathRecursively(exportsObj: Exports, matcher: (path: string) => boolean, key?: string): {
    key: string;
    value: Exports;
} | undefined;
declare function _externalName(pkg: PkgJSON, relativePath: string): string | undefined;
export declare const externalName: typeof _externalName;
export declare function _matches(candidate: string, relativePath: string): boolean;
export {};
