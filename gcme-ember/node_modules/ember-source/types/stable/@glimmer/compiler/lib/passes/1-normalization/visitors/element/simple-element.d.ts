declare module '@glimmer/compiler/lib/passes/1-normalization/visitors/element/simple-element' {
    import type { ASTv2, SourceSlice } from "@glimmer/syntax";
    import type { Result } from "@glimmer/compiler/lib/shared/result";
    import type { Classified, ClassifiedElement, PreparedArgs } from "@glimmer/compiler/lib/passes/1-normalization/visitors/element/classified";
    import * as mir from "@glimmer/compiler/lib/passes/2-encoding/mir";
    export class ClassifiedSimpleElement implements Classified {
        private tag;
        private element;
        readonly dynamicFeatures: boolean;
        constructor(tag: SourceSlice, element: ASTv2.SimpleElement, dynamicFeatures: boolean);
        readonly isComponent = false;
        arg(attr: ASTv2.ComponentArg): Result<mir.NamedArgument>;
        toStatement(classified: ClassifiedElement, { params }: PreparedArgs): Result<mir.Statement>;
    }
}