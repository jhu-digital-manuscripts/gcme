declare module '@glimmer/runtime/lib/vm/content/text' {
    import type { SimpleText, UpdatingOpcode } from "@glimmer/interfaces";
    import type { Reference } from "@glimmer/reference";
    export default class DynamicTextContent implements UpdatingOpcode {
        node: SimpleText;
        private reference;
        private lastValue;
        constructor(node: SimpleText, reference: Reference, lastValue: string);
        evaluate(): void;
    }
}