import { type IDOMElementDescriptor } from 'dom-element-descriptors';
/**
 * @ignore
 * Create an {@link IDOMElementDescriptor} from a target and a root element
 */
export default function createDescriptor(target: string | Element | null | IDOMElementDescriptor | undefined, rootElement: RootElement): IDOMElementDescriptor;
