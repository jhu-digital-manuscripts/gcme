{
  "settings": {
    "index" : {
      "refresh_interval" : "10s"
    },
    "analysis": {
      "char_filter" : {
        "remove_complex_tags" : {
          "type": "pattern_replace",
          "pattern": "[%#]\\S+",
          "replacement": ""
        }
      },
      "analyzer": {
        "tagged_lemmas": {
          "type": "custom",
          "tokenizer": "whitespace",
          "char_filter": ["remove_complex_tags"],
          "filter": ["lowercase", "asciifolding"]
        }
      }
    }
  },
  "mappings": {
    "_doc": {
      "dynamic": false,      
      "properties": {
        "id": {"type": "keyword"},
        "number": {"type": "integer"},
        "raw_number": {"type": "keyword"},        
        "text": {"type": "text", "analyzer": "whitespace"},
        "tag_lemma_text": {"type": "text", "analyzer": "tagged_lemmas"},        
        "group": {"type": "keyword"}
      }
    }
  }
}
